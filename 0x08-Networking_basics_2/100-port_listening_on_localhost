#!/bin/bash

# Check if nc (netcat) command is available
if ! command -v nc &> /dev/null; then
    echo "Error: netcat (nc) command is not available."
    exit 1
fi

# Listen on port 98 on localhost
echo "Listening on port 98 on localhost..."
nc -l -p 98 -vv
